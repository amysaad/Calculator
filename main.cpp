/* Name: Amy Saad
 * Files: main.cpp, stack.h, stack.cpp, expression.h, expression.cpp
 *
 * Program: Simulate a calculator that can handle mathematical operators with floating point operands
 * Input: one line calculation in infix order of any length, with valid operators are +, −, /, %, *, and ^.
 * The = operator signifies the end of the expression.
 * Output: The answer of the calculation with 3 decimal places
 */

#include "expression.h"
using namespace std;

int main() {
    Expression expression;    //instance of expression class
    return 0;
}
